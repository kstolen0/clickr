services:
  clickr:
    build: .
    container_name: clickr
    networks:
      - pi
    ports:
      - 8081:8080
    volumes:
      - app-data:/data

networks:
  pi:
    external: true

volumes:
  app-data:
    driver: local
